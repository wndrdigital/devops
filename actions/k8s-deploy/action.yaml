name: "Kubernetes Deploy"
description: "Deploy application to Kubernetes with Infisical operator secrets"

inputs:
  namespace:
    description: "Kubernetes namespace"
    required: true
  app-name:
    description: "Application/deployment name"
    required: true
  image:
    description: "Full GHCR image name for web"
    required: true
  cli-image:
    description: "Full GHCR image name for CLI (queue/migrate). Defaults to image value."
    required: false
    default: ""
  ghcr-pat:
    description: "GHCR Personal Access Token for image pull secret"
    required: true
  k8s-dir:
    description: "Path to k8s manifests directory"
    required: false
    default: "k8s"

runs:
  using: "composite"
  steps:
    - name: Apply namespace
      shell: bash
      run: kubectl apply -f ${{ inputs.k8s-dir }}/namespace.yaml

    - name: Create image pull secret
      shell: bash
      run: |
        kubectl create secret docker-registry ghcr-login-secret \
          --namespace=${{ inputs.namespace }} \
          --docker-server=ghcr.io \
          --docker-username=wndrdigital \
          --docker-password=${{ inputs.ghcr-pat }} \
          --dry-run=client -o yaml | kubectl apply -f -

    - name: Apply Infisical secret sync
      shell: bash
      run: |
        if [ -f "${{ inputs.k8s-dir }}/infisical-secret.yaml" ]; then
          kubectl apply -f ${{ inputs.k8s-dir }}/infisical-secret.yaml
        fi

    - name: Update image tags
      shell: bash
      run: |
        CLI_IMAGE="${{ inputs.cli-image }}"
        if [ -z "$CLI_IMAGE" ]; then
          CLI_IMAGE="${{ inputs.image }}"
        fi

        for f in ${{ inputs.k8s-dir }}/*.yaml; do
          sed -i "s|${{ inputs.image }}:latest|${{ inputs.image }}:${{ github.sha }}|g" "$f"
          sed -i "s|${CLI_IMAGE}:latest|${CLI_IMAGE}:${{ github.sha }}|g" "$f"
        done

    - name: Run database migrations
      shell: bash
      run: |
        if [ -f "${{ inputs.k8s-dir }}/migrate-job.yaml" ]; then
          kubectl delete job ${{ inputs.app-name }}-migrate -n ${{ inputs.namespace }} --ignore-not-found
          kubectl apply -f ${{ inputs.k8s-dir }}/migrate-job.yaml
          kubectl wait --for=condition=complete job/${{ inputs.app-name }}-migrate -n ${{ inputs.namespace }} --timeout=120s
        fi

    - name: Apply manifests
      shell: bash
      run: |
        kubectl apply -f ${{ inputs.k8s-dir }}/

    - name: Wait for rollout
      shell: bash
      run: |
        kubectl rollout status deployment/${{ inputs.app-name }} -n ${{ inputs.namespace }} --timeout=300s
